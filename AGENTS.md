# Working agreement

- Markdown/YAML-LD is the source of truth; regenerate projections after edits.
- Preserve stable IDs, aliases, links and route collision checks.
- Keep docs, semantic descriptors, Explorer JSON and checksums synchronized.
- Run bundle, publication and semantic tests before committing.
